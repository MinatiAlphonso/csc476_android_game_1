<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.sdsmt.group2">

    <application
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_adaptive_fore"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Group2">
        <activity
            android:launchMode="singleInstance"
            android:name="edu.sdsmt.group2.CaptureActivity"
            android:exported="false" />
        <activity
            android:launchMode="singleInstance"
            android:name="edu.sdsmt.group2.GameActivity"
            android:exported="false" />
        <activity
            android:launchMode="singleInstance"
            android:name="edu.sdsmt.group2.EndActivity"
            android:exported="false" />
        <activity
            android:launchMode="singleInstance"
            android:name="edu.sdsmt.group2.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>